// Copy this file to "wifi_secrets.h" and fill in your WiFi credentials.
// "wifi_secrets.h" is gitignored and will never be committed.
// Alternatively, provision credentials at runtime via serial:
//   WIFI_SSID=<ssid>  then  WIFI_PASS=<password>

#ifndef WIFI_SECRETS_H
#define WIFI_SECRETS_H

#define WIFI_DEFAULT_SSID ""
#define WIFI_DEFAULT_PASS ""

#endif
